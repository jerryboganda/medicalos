import { defineConfig } from 'astro/config';

// Marketing site (ADR 0002): landing, per-exam pages, pricing, checkout,
// help, legal. Holds no learner data; calls only public/checkout endpoints.
export default defineConfig({});
